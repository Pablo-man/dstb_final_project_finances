name: Registry image

on:
  push:
    branches: ["QA"]

jobs:
  login-and-pull-dev:
    if: github.ref == 'refs/heads/dev'
    uses: Pablo-man/dstb_final_project_finances/usersAdministration/createUser/.github/workflows/login_pull.yml@QA
    secrets:
      docker_password: ${{ secrets.DOCKER_PASSWORD }}
      docker_username: ${{ secrets.DOCKER_USERNAME }}
      repository_name: ${{ secrets.DOCKER_REPOSITORY_USERCREATE }}
